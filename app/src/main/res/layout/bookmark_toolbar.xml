<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.Toolbar android:id="@+id/toolbarBook"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    android:background="@color/main_Color"
    android:gravity="center_vertical|start"
    app:popupTheme="@style/AppTheme.PopupOverlay"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto" >

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageButton
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:id="@+id/bookmarkMapsButton"
            android:scaleType="fitCenter"
            android:adjustViewBounds="true"
            app:srcCompat="@mipmap/ic_bookmark_border_white_24dp"
            android:background="@color/main_Color"/>

        <ImageButton
            android:layout_width="0dp"
            android:layout_height="match_parent"
            android:layout_weight="1"
            android:id="@+id/faciMapsButton"
            android:scaleType="fitCenter"
            android:adjustViewBounds="true"
            app:srcCompat="@mipmap/ic_pets_white_24dp"
            android:background="@color/main_Color"/>

    </LinearLayout>
</android.support.v7.widget.Toolbar>

